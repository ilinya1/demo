# 学生宿舍管理系统 — 项目交接文档

> 版本记录：v1 → v2 → v3 → v4 → **v5**
> 交接对象：后续接手本项目的开发 / 维护人员
> 说明：本仓库当前交付阶段：**Vue3 前端已完成纯前端开发（内置 mock 数据服务，可脱离后端独立运行演示）** + **数据库已建库运行（MySQL** **`dorm_manager`，11 张表）** + **Spring Boot 后端骨架（尚未业务化）**。本文档记录各版本已做内容、前端模块全貌、数据库设计全貌、当前技术状态、以及后续开发待办。

***

## 一、项目概览

- **系统名称**：学生宿舍管理系统

- **定位**：高校宿舍的入住退宿、卫生检查、报修管理等信息化管理平台

- **当前形态**：

  - 前端主实现：**Vue 3 单页应用**（`frontend/`），内置 **mock 数据服务**模拟全部后端接口，可脱离后端独立运行演示；`prototype/` 为早期静态 HTML 原型，保留作设计参考与字段基线

  - 后端：Spring Boot 4.1.1 骨架（`src/`，**尚未业务化**）

  - 数据库：**已建库运行** `dorm_manager`（MySQL80），11 张表（`docs/sql/init.sql` + `docs/数据库设计说明.md` 已同步并通过终核）

- **运行方式（前端）**：`frontend/` 下执行 `npm install && npm run dev`，访问 `http://localhost:3000`。页面内使用内置 mock 数据模拟全部接口，**无需后端即可完整演示**。演示账号：管理员 `admin/123456`、学生 `2023010101/123456`。

- **技术栈（前端）**：Vue ^3.5、Vite ^6、Element Plus ^2.14、Pinia ^3、Vue Router ^4.5、Axios ^1.7、ECharts ^6（统计报表）；后端 Java 21 + Spring Boot 4.1.1（计划引入 `mybatis-plus-spring-boot4-starter` + MySQL 8）。

### 目录结构

```
demo/
├── pom.xml                       Maven，spring-boot-starter-webmvc（Java 21）
├── src/main/java/com/example/demo/DemoApplication.java   后端启动类（骨架，尚未业务化）
├── src/main/resources/application.yaml
├── frontend/                     前端主实现（Vue 3 + Vite + Element Plus）
│   ├── package.json              dev/build/preview 脚本（Vite ^6）
│   ├── vite.config.js / index.html / src/main.js
│   └── src/
│       ├── layouts/              AdminLayout / StudentLayout（双端侧边栏布局）
│       ├── views/                页面视图（admin 16 / student 6 / Login、PreviewDemo）
│       ├── components/           公共组件（如 PasswordForm 修改密码）
│       ├── api/                  接口封装（request 解包、mock 分发、token/401 拦截）
│       ├── mock/                 内置 mock 服务（authData/baseData/checkin/daily/settings/stats/index）
│       ├── stores/               Pinia（user 登录态）
│       └── router/               路由 + 角色守卫
├── prototype/                    早期静态 HTML 原型（设计参考、字段基线，保留）
└── docs/
    ├── 项目交接文档.md            本文档（v5）
    ├── 数据库设计说明.md          数据库字段字典/ER/枚举/约定（与 init.sql 同步）
    ├── sql/init.sql              数据库建表脚本（11 张表 + 索引 + 示例数据）
    └── HTML原型修改记录.md        审计 + 操作日志（#1~#32，持续更新）
```

### 角色与入口

- **管理员（宿管）**：`index.html` → `admin/login.html`（账号/密码）→ `admin/dashboard.html`

- **学生**：`index.html` → `student/login.html`（学号/密码，示例 **2023010101 / 123456**）→ `student/my-room.html`

***

## 二、v1 —— 基础原型（核心模块搭建）

> 状态：已完成 ✅

搭建完整页面骨架与导航框架，确立"管理员端 + 学生端"双端结构。

**管理员端（`prototype/admin/`，侧边栏 13 个模块）**

| 分组   | 模块         | 主要页面                                        | 说明                     |
| ---- | ---------- | ------------------------------------------- | ---------------------- |
| 概览   | 仪表盘        | dashboard                                   | 在校学生数/楼栋/房间/入住率等统计卡片   |
| 基础数据 | 学生管理       | student-list                                | 学生列表、搜索、新增/编辑、导入/导出    |
| 基础数据 | 班级管理       | class-list                                  | 班级列表、搜索、新增/编辑          |
| 基础数据 | 楼栋管理       | building-list                               | 楼栋增删改查                 |
| 基础数据 | 房间管理       | room-list / room-detail                     | 房间列表、新增/编辑、床位分布详情      |
| 住宿业务 | 入住登记       | checkin                                     | 学号带出学生 → 选楼栋/房间/床位办理入住 |
| 住宿业务 | 退宿处理       | checkout-audit                              | 退宿申请审核 + 直接退宿          |
| 住宿业务 | 入住记录       | checkin-record                              | 入住/退宿记录、状态筛选、退宿入口      |
| 日常管理 | 卫生检查       | hygiene-list / hygiene-add                  | 检查记录列表 / 登记            |
| 日常管理 | 报修管理       | repair-list / repair-detail                 | 报修列表 / 报修处理（派单）        |
| 统计报表 | 入住/卫生/报修统计 | stat-occupancy / stat-hygiene / stat-repair | 三张统计报表                 |

**学生端（`prototype/student/`，侧边栏 5 个模块）**

| 分组   | 模块     | 主要页面           | 说明               |
| ---- | ------ | -------------- | ---------------- |
| 我的宿舍 | 我的入住信息 | my-room        | 宿舍信息 + 我的信息 + 室友 |
| 我的宿舍 | 我的卫生检查 | my-hygiene     | 查看自己卫生检查成绩/评语    |
| 我的宿舍 | 退宿申请   | checkout-apply | 提交退宿申请、查看进度、撤销   |
| 报修服务 | 提交报修   | repair-add     | 填写报修提交           |
| 报修服务 | 报修进度   | my-repair      | 查看报修处理进度/处理人     |

**公共能力**：`common.css` 全套样式（布局/卡片/表格/表单/弹窗/标签/照片堆叠）；`common.js` 的 `showMsg`、`openModal/closeModal`、菜单高亮自动匹配等。

> 说明：v1 以"页面齐全、导航一致、可浏览"为目标，功能以静态占位为主（按钮多为 alert 提示）。

***

## 三、v2 —— 功能增强迭代

> 状态：已完成 ✅

在 v1 基础上补齐业务闭环与交互细节，重点是"学生端 ↔ 管理员端"的数据流转。

### v2.1 班级管理（新增）

- 新增 `admin/class-list.html`：班级列表（班级名称/所属学院/专业/年级/学生人数/住宿人数/班主任）、新增/编辑弹窗、班级搜索

- 全部管理员页面侧边栏"基础数据"中，于"学生管理"与"楼栋管理"之间加入 **班级管理（🏫）** 入口

### v2.2 退宿申请 — 审核流程（重构为统一流程）

- 新增学生端 `checkout-apply.html`：申请表单（当前宿舍/入住时间只读，选退宿原因/计划日期/说明）+ 我的申请记录（含状态、待审核可撤销）

- 新增管理员端 `checkout-audit.html`：申请列表 + 审核弹窗（通过/驳回，驳回需填意见）

- **统一流程**：移除原"退宿登记"入口，删除 `admin/checkout.html`，强制走"学生申请 → 管理员审核"单一流程

### v2.3 班级输入改为下拉框

- `student-list`（新增/编辑学生）、`class-list`（搜索栏）的班级文本框 → 下拉（软工2301/软工2302/计科2301/机设2301/英语2201）

- 保留"新增班级"弹窗班级名为文本框（需自定义新名）

### v2.4 卫生检查：上传图片 + 可靠评分

- `hygiene-add.html`：现场照片上传（多张 + 缩略图预览）；扣分项自动计分（地面-5/桌面-3/垃圾-5/被子-3/违规电器-15，100 起扣）；自动判定（≥90 优秀 / ≥60 合格 / <60 不合格）；**低分/违规强制传照片**

- `hygiene-list.html` 增加照片列（缩略图）+ 大图查看；`common.css` 新增 `.photo-thumb/.photo-stack/.photo-count`

### v2.5 报修处理人联系方式

- `repair-list.html` 列表新增 **处理人 / 联系电话** 列

- `repair-detail.html` 处理弹窗新增 **联系电话** 输入（派单时填写）

- `my-repair.html` 进度列表新增 **处理人（联系电话）** 列

- 数据闭环：详情派单填姓名+电话 → 管理员列表 + 学生进度同步回显

### v2.6 退宿处理 + 直接退宿

- 文案统一：菜单/标题"退宿审核" → **退宿处理**（全站 0 残留）

- `checkout-audit.html` 顶部新增 **＋ 直接退宿**：输学号带出姓名/当前宿舍（只读）→ 选退宿日期/原因/备注 → 跳过申请立即办理并空出床位（含红字提示）

### v2.7 导入 / 导出（防错）

- 新增导入模板 `templates/学生信息导入模板.csv`（UTF-8 带 BOM，表头与导入字段一致，含示例行）

- `student-list.html` "批量导入"旁新增 **⬇ 下载模板**

- 全部"批量导出"接入真实 CSV 下载：`common.js` 新增 `exportTableCSV()`（UTF-8 BOM、逗号/引号/换行转义、跳过操作列）；student-list → `学生列表.csv`、class-list → `班级列表.csv`

***

## 四、v3 —— 全面审计与交接基线

> 状态：已完成 ✅（产出：审计结论 + 字段基线）

### v3.1 全项目一致性审计

对 `prototype/` 全部页面做"逻辑闭环 + 字段一致性"核对（详见《HTML原型修改记录.md》#4）：

- **导航闭环通过**：断链 0、孤儿页面 0、管理员各页菜单 13、学生各页 5

- **端到端字段闭环通过**：报修/退宿/卫生的学生提交 ↔ 管理员处理 ↔ 学生回显，字段及状态枚举一致

### v3.2 修复的不一致（1 处）

- `checkin-record.html` 列名 `状态`（承载住宿状态）与 `student-list` 的 `状态`（学籍）同名不同义 → 统一改 **`住宿状态`**；住宿状态统一枚举 `在住/已退宿/未住`

- 全部关键改动已在《HTML原型修改记录.md》以操作日志逐步记录（#1\~#4），保证可追溯

***

## 五、v4 —— 数据库设计落地与终核

> 状态：已完成 ✅（产出：`docs/sql/init.sql` + `docs/数据库设计说明.md`，并经过 4 轮复核）

### v4.1 设计决策（核心，后续后端必须遵守）

| 决策                    | 内容                                                                                                                          |
| --------------------- | --------------------------------------------------------------------------------------------------------------------------- |
| **逻辑外键**              | 关联字段以 `_id`（或 `student_id`/`order_no` 等业务键）表示，**不建物理 FOREIGN KEY**；完整性由后端 Service 在事务内校验兜底。代价是数据库不兜底，故所有写接口必须走统一 Service 校验 |
| **快照冗余（仅 check\_in）** | 住宿历史为核心审计读路径，入住时写入 `student_name/class_name/building_name/room_no/bed_no` 文本快照，**退宿不覆盖**；卫生/报修房间号稳定，不加快照                    |
| **冗余联动**              | `student.housing_status`（在住/已退宿/未住）冗余在 student 表，由入住/退宿 Service 同步维护，避免每页 join                                              |
| **床位独立建模**            | `dorm_bed` 独立表（room\_id + bed\_no + status），支撑 room-detail 床位分布，二期调宿/维修床可复用                                                 |
| **账号体系**              | `sys_user` 统一承载管理员与学生登录；学生 `username` 与学号同值并关联 `student_id`，密码存 BCrypt（演示样本 `{noop}` 明文）                                    |
| **多图存储**              | `hygiene_record.photos`、`repair_order.images` 以 TEXT 存路径列表(JSON)，一期本地目录、二期 OSS                                              |
| **二期预留策略**            | 不预留死字段；`notice/水电/违纪` 二期再建表；`check_in` 天然支持调宿、`sys_user.role` 可扩 TEACHER                                                    |

### v4.2 数据库全貌（10 张表）

|  #  | 表名               | 说明           | 关键字段/约定                                                                                                                                      |
| :-: | ---------------- | ------------ | -------------------------------------------------------------------------------------------------------------------------------------------- |
|  1  | `class`          | 班级主数据        | class\_name 唯一；college/major/grade/head\_teacher                                                                                             |
|  2  | `student`        | 学生           | PK=student\_id(学号)；class\_id→class；academic\_status + housing\_status(冗余)；contact\_phone                                                     |
|  3  | `sys_user`       | 登录账号         | username 唯一、student\_id 唯一；role=ADMIN/STUDENT                                                                                                |
|  4  | `dorm_building`  | 楼栋           | building\_name 唯一；floor\_count/room\_count/manager                                                                                           |
|  5  | `dorm_room`      | 房间           | building\_id→楼栋；唯一(building\_id, room\_no)；capacity/room\_type/status                                                                        |
|  6  | `dorm_bed`       | 床位           | room\_id→房间；唯一(room\_id, bed\_no)；status=空闲/占用/维修                                                                                            |
|  7  | `check_in`       | **住宿记录（核心）** | 关联 student/building/room/bed（均 NOT NULL）+ 5 快照字段；check\_in\_time/check\_out\_time/source/status                                              |
|  8  | `checkout_apply` | 退宿申请         | apply\_no 唯一；reason/plan\_date/description/status/reject\_reason/create\_time/audit\_time                                                    |
|  9  | `hygiene_record` | 卫生检查         | check\_date/checker/building\_id/room\_id(均 NOT NULL)/score/result/deduct\_items(JSON)/photos(JSON)/comment                                  |
|  10 | `repair_order`   | 报修           | order\_no 唯一；student/building/room(均 NOT NULL)；item/description/contact\_phone/images(JSON)/status/handler\_name/handler\_phone/handle\_desc |

> 字段级完整字典、ER 关系、索引、枚举字典见《数据库设计说明.md》；建表语句与示例数据见 `init.sql`。

### v4.3 多轮复核与修复（A\~F 类）

四轮复核累计修复问题（详见《HTML原型修改记录.md》#7\~#11）：

- **A 类（必须修）**：样例数据矛盾（房间"部分入住"却无人无床）→ 补入住样例复刻原型；密码标注与示例冲突（`{noop}` vs BCrypt）→ 澄清为"演示明文占位、生产 BCrypt"；文档 `student.id`→`student.student_id` 笔误

- **B 类（字段级加固）**：`check_in` 的 building/room/bed\_id 改 `NOT NULL`；`sys_user.student_id` 加唯一约束

- **C 类（严格度统一）**：`hygiene_record`/`repair_order` 的 building/room\_id 改 `NOT NULL`（与 check\_in 一致，原型均必填）；给 `check_in` 补 `(status, check_out_time)` 退宿统计索引

- **E 类（前端枚举对齐）**：`student-list.html` 住宿状态"已退"→"已退宿"，全站残留清零

- **F 类（约定补充）**：退宿申请待审核可"撤销"（直接删除记录，不加枚举）

***

## 六、v5 —— Vue3 前端开发落地与数据库修订

> 状态：已完成 ✅（产出：`frontend/` 完整 Vue3 单页应用，内置 mock 数据服务，可独立运行；数据库修订为 11 表并存库运行）

### v5.1 前端从静态原型升级为 Vue3 工程

在保留 `prototype/` 静态原型作为字段基线的前提下，新增 `frontend/` 工程，将全部业务页面实装为可交互组件，并内置 **mock 数据服务**模拟后端接口，使前端**不依赖后端也可完整走通全部业务闭环**。

**管理员端（16 个视图）**

| 分组   | 模块          | 页面/能力                                                                         |
| ---- | ----------- | ----------------------------------------------------------------------------- |
| 概览   | 仪表盘         | Dashboard：在校/楼栋/房间/入住率等统计卡片                                                   |
| 基础数据 | 学生/班级管理（合并） | StudentList：左侧班级面板（搜索/选中高亮/增删改/班内统计）+ 右侧该班学生（筛选/分页/增删改），班内新增自动预填班级，删除有学生班级被拦截 |
| 基础数据 | 楼栋管理        | BuildingList                                                                  |
| 基础数据 | 房间管理        | RoomList：房间列表 + 床位分布                                                          |
| 住宿业务 | 入住登记        | Checkin：4 步（学号带出学生 → 选楼栋/房间/床位），校验未在住+床位空闲，联动床位/房间/住宿状态                       |
| 住宿业务 | 退宿处理        | CheckoutAudit：申请审核（通过/驳回）+ 直接退宿（source=direct）                                |
| 住宿业务 | 入住记录        | CheckinRecord：入住/退宿记录，按状态/学号/姓名/楼栋筛选                                          |
| 日常管理 | 卫生检查        | HygieneList / HygieneAdd：照片+评分自动判定                                            |
| 日常管理 | 报修管理        | RepairList + RepairType：报修处理（派单）、报修类型字典维护                                     |
| 统计报表 | 入住/卫生/报修统计  | StatOccupancy / StatHygiene / StatRepair（ECharts）                             |
| 系统设置 | 全局设置        | Settings：Tab「系统参数」（名称/欢迎语/联系电话/邮箱，可保存/恢复默认）+「退宿原因字典」CRUD                      |
| 个人中心 | 个人资料        | Profile：资料展示 + 编辑联系电话/邮箱 + 修改密码                                               |

**学生端（6 个视图）**

| 分组   | 模块   | 页面/能力                              |
| ---- | ---- | ---------------------------------- |
| 我的宿舍 | 入住信息 | MyRoom：宿舍信息 + 我的信息 + 室友            |
| 我的宿舍 | 卫生检查 | MyHygiene：查看自己卫生成绩/评语              |
| 我的宿舍 | 退宿申请 | CheckoutApply：提交/查进度/撤销，退宿原因下拉取字典  |
| 报修服务 | 提交报修 | RepairAdd：填写报修（类型取字典）              |
| 报修服务 | 进度查询 | MyRepair：查看处理进度/处理人                |
| 个人中心 | 个人资料 | Profile：资料展示 + 编辑联系电话/紧急联系人 + 修改密码 |

**公共工程能力**

- 统一 `request.js`：解包 `{code,data}`、mock/axios 双模式分发、token 注入与 401 拦截

- 路由 + 角色守卫（ADMIN/STUDENT 分流，未登录跳 /login）；Pinia `user` 登录态持久化

- 全局注册 Element Plus 图标；公共组件化（如修改密码 `PasswordForm` 被 admin/student 复用）

- 系统参数生效：登录页品牌名/欢迎语、双端侧边栏品牌名读取系统参数，改后可即时反映

### v5.2 内置 mock 服务架构（需与后端接口对齐）

`frontend/src/mock/` 模块化模拟后端，**是后续后端接口契约的唯一前端镜像**：

- `authData.js`（登录账号/token）、`baseData.js`（班级/学生/楼栋/房间/床位联动）、`checkin.js`（入住/退宿/申请审核）、`daily.js`（卫生/报修）、`settings.js`（系统参数/字典/个人资料/改密）、`stats.js`（统计）、`index.js`（端点分发）

- 依赖方向单向无环：`authData → util`、`settings → authData/baseData`、`index → 各模块`

### v5.3 关键设计 / 修复（接管时必须知悉）

- **模块环引用修复**：账号数据抽为独立 `authData.js`，消除 `settings ↔ index` 相互依赖

- **班级改名级联**：合并页班级改名时同步更新该班学生 `className`（快照关联键），避免学生"孤立"且保证删除引用校验有效；改名同名被拦截

- **字典删除引用拦截**：报修类型被报修单引用、退宿原因被申请引用时禁止删除

- **角色硬编码（已知事项）**：admin/student 的 Profile 分别写死 `role`，各自挂在对应布局下可接受；若未来共用组件需改为从 store 读取

### v5.4 数据库修订（10 → 11 表）

- 新增 `repair_type` 字典表（`id`/`name`/`sort`，`name` 唯一），内置数据：灯管/水龙头/空调/门锁/床铺/桌椅/其他

- `repair_order` 用 `type_id`（逻辑外键 → `repair_type.id`）取代原 `item` 字段

- `docs/sql/init.sql` 与 `docs/数据库设计说明.md` 已同步；已在本机 MySQL80 执行建库 `dorm_manager` 并验证 11 表结构

***

## 七、当前技术状态 / 已知事项

- **前端**：Vue 3 工程已完成全部业务页面 + 内置 mock，`npm run build` 通过，dev server（端口 3000）可独立运行演示。**当前所有"写"操作仅落在内存 mock 数据上，刷新页面即还原，未持久化、未接真实后端。**

- **数据库**：已建库运行 `dorm_manager`（MySQL80，11 表）。`docs/sql/init.sql` 与 `docs/数据库设计说明.md` 需**始终保持同步**（改一处必须改另一处）。示例数据与原型 `room-detail`（1号楼 102 室 3 人在住）一致，便于联调对照。

- **后端**：`src/` 仅启动类 `DemoApplication.java` 与空 `application.yaml`，**无 MyBatis/JPA 依赖、无实体、无接口**（pom 仅 webmvc），尚未业务化。

- **prototype/**：早期静态 HTML 原型，保留作字段基线/设计参考；与 Vue 前端可能在某些交互细节上略滞后，以后端联调以 **Vue 前端 + mock 契约**为准。

- **登录为演示**：`admin/123456`、学生 `2023010101/123456`；`{noop}` 明文占位（生产用 BCrypt）。

- **统计页**：当前为 mock 静态示例聚合，非后端实时计算。

***

## 八、后续开发待办

> 以下均以 v5 数据库设计（`init.sql` / `数据库设计说明.md`）为唯一字段基线；前端接口契约以 `frontend/src/mock/` 为镜像对齐。

### A. 后端接口实现（Spring Boot + MyBatis-Plus）——最高优先

1. **工程引入**：pom 增加 `mybatis-plus-spring-boot4-starter`（3.5.x，Spring Boot 4 专用，**禁用** `mybatis-plus-boot-starter`/`boot3` starter）、`mysql-connector-j`；`application.yaml` 配 mysql 连接 + 逻辑删除/驼峰映射；本地 SQL SDK 设为 JDK21（系统 JAVA\_HOME 仍为 1.8，需 IDE 设 Project SDK 21）
2. **实体映射**：按《数据库设计说明.md》11 张表逐一建立实体（注意 `_id` 逻辑外键、`check_in` 快照字段、`repair_order.type_id`、`comment` 预留字转义）
3. **统一返回/异常**：定义 `Result<T>`、分页对象、业务异常；Service 层统一事务 + 逻辑外键校验入口（接口完整性兜底，数据库不建物理外键）
4. **模块接口**（每个模块按 admin/student 分）：

   - 登录鉴权：`sys_user` 校验 + JWT/会话 + 按角色路由

   - 基础数据 CRUD：学生、班级、楼栋、房间（`capacity` 与 bed 行数一致由 Service 保证）、床位

   - 入住/退宿：入住（校验未在住+床位空闲，写 `check_in` 快照 + 联动床位/住房状态）、退宿申请（提交/撤销**删除记录**/审核）、**直接退宿**（source=direct）、入住记录查询

   - 卫生检查 CRUD（扣分项/照片、结果自动判定）、报修 CRUD（类型取 `repair_type` 字典、派单写 handler）、学生查进度

   - 系统设置：系统参数、字典（报修类型/退宿原因）增删改（**删除引用校验**）

   - 个人中心：个人资料查询/更新、修改密码

   - 统计：dashboard 指标 + 三张统计报表（按表实时聚合）

### B. 前端联调（`frontend` 由 mock 换真实接口）

1. 以 v5 字段基线核查接口字段命名与原型一致（`contact_phone` 统一、`repair_order.type_id`、住宿状态枚举 `在住/已退宿/未住` 等）
2. 前端 `api/request.js` 由 mock 分发切换到真实 axios 基址；逐模块打通
3. **图片上传落地**：卫生照片、报修图片由本地预览改真实上传（本地目录或 OSS），持久化路径至 `photos`/`images`，列表渲染缩略图

### C. 工程化与功能完善

1. **导入导出后端化**：学生批量导入 CSV 真实解析入库（校验字段/枚举），导出由后端生成
2. **统计真实化**：dashboard 与三张统计报表改后端按表实时聚合
3. **可扩展**：调宿（换床位）、毕业批量退宿、班主任账号、报修催单/评价、卫生周期提醒

### D. 交付与部署（收尾）

1. `init.sql` 在目标 MySQL 执行无误；README/交接文档补全启动（前端 dev、后端、建库）与部署说明
2. 明确演示账号（admin/123456、2023010101/123456）与演示数据，保证一键可演示

***

## 九、版本修订记录

| 版本 | 时间         | 变更内容                                                                                                                      |
| -- | ---------- | ------------------------------------------------------------------------------------------------------------------------- |
| v1 | —          | 基础原型：双端页面骨架、13+5 模块导航、公共样式/脚本、核心业务页面                                                                                      |
| v2 | —          | 功能增强：班级管理、退宿申请-审核统一流程、班级下拉、卫生照片+评分、报修处理人电话、退宿直接办理、导入导出                                                                    |
| v3 | 2026-09-03 | 全面审计与字段基线：导航/闭环核查、修复住宿状态列名、建立建库字段映射、追加操作日志                                                                                |
| v4 | 2026-09-03 | 数据库设计落地与终核：10 表 DDL + 设计说明文档、逻辑外键/快照/冗余约定、字段级加固、样例数据自洽，并纳入后续开发基线                                                          |
| v5 | 2026-09-04 | Vue3 前端开发落地：完整双端 SPA + 内置 mock 服务、全局设置/个人中心、学生与班级合并、环引用与班级改名级联修复；数据库修订为 11 表（新增 repair\_type、repair\_order.type\_id）并建库运行 |

